# 开发环境配置(配置项只包括环境不同项)
spring:
    datasource:
        url: jdbc:mysql://127.0.0.1:3306/system_a?useUnicode=true&characterEncoding=utf-8&useSSL=false
        driver-class-name: com.mysql.jdbc.Driver
        username: root
        password: root

    activemq:
        broker-url: tcp://127.0.0.1:61616
        user: admin
        password: admin

logging:
  level:
    org.springframework:
      data: DEBUG
      web: DEBUG
      jms: DEBUG
    tk.mybatis: DEBUG


